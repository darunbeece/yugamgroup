version: '3.8'

services:
  yugam-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yugam-group-website
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - yugam-network

networks:
  yugam-network:
    driver: bridge
